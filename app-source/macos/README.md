# macOS source

The native macOS version is written in Swift and SwiftUI for Apple silicon.

To run a development build, install Xcode command-line tools, open Terminal in this folder, and run `swift run`.

`build.sh` creates the packaged app used for distribution. The app icon source is included under `Resources`.
